类之间的关系：继承，实现，依赖，组合聚合
创建对象  类名+对象名 =new 类名（）
调用方法：  对象.    可直接调用
递归的调用在栈里面调用   （无出口的话会一直调用直到栈的内存用完）
classpath的含义：类的路径   
classpath的作用：在没有用idear的时候，比如直接在Java控制台，需要通过配置classpath来告诉系统类的路径
在哪里
Java 在控制台中运用Scanner 类可输入数字，字符等  （java.util）
Java中数据类型分为基本数据类型和引用数据类型
引用数据类型：类，接口，数组
如何判断一个字符串中是否包含我们期望的字符：contains 和indexof都可以。
如何在一段已有的字符串截取我们需要的字符：charAt 可得到一个     substring 既可得到一个也可以得到多个
方法传参的方式：值传递和地址传递（引用传递） 字符串虽然是类，但是传递的时候也是值传递。
封装的步骤：1、变量加private    2、set get 
方法结束的标志：return（没有返回值也return结束）
成员变量：类中的变量是成员变量
局部变量：方法中的变量是局部变量
javac可以将原代码编译
什么是方法，方法的概念：可以通过调用一段特定的代码来实现自己想要的特定功能
                                        （实现特定功能的代码的集合）


